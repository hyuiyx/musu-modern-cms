const htmlHeaders={"content-type":"text/html; charset=utf-8"};
const esc=(v="")=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const json=(v,s=200)=>new Response(JSON.stringify(v),{status:s,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
const slugify=(v="")=>String(v).toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");
async function rows(db,sql,...a){return (await db.prepare(sql).bind(...a).all()).results||[]}
async function one(db,sql,...a){return await db.prepare(sql).bind(...a).first()}
async function siteData(env){const s=Object.fromEntries((await rows(env.DB,"SELECT key,value FROM settings")).map(x=>[x.key,x.value]));const cats=await rows(env.DB,"SELECT id,name,slug FROM categories WHERE status='published' ORDER BY sort_order,id");return {s,cats}}
function nav(cats){const pc=cats.map(c=>`<a href="/products/?category=${esc(c.slug)}">${esc(c.name)}</a>`).join("");return `<nav class="navlinks"><a href="/">Home</a><a href="/about/">About</a><div class="drop"><a href="/products/">Products ▾</a><div class="dropmenu"><a href="/products/">All Products</a>${pc}</div></div><div class="drop"><a href="/news/">News ▾</a><div class="dropmenu"><a href="/news/">All News</a><a href="/news/?type=company_news">Company News</a><a href="/news/?type=industry_news">Industry News</a></div></div><div class="drop"><a href="/cases/">Cases ▾</a><div class="dropmenu"><a href="/cases/">All Cases</a></div></div><div class="drop"><a href="/videos/">Videos ▾</a><div class="dropmenu"><a href="/videos/">All Videos</a></div></div><div class="drop"><a href="/contact/">Contact ▾</a><div class="dropmenu"><a href="/contact/">Contact Us</a><a href="/feedback/">Feedback</a></div></div></nav>`}
function shell(meta,body,d){return new Response(`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(meta.title||'SMUSU')}</title><meta name="description" content="${esc(meta.desc||'Professional diamond tools')}"><link rel="canonical" href="${esc(meta.canonical||'')}"><link rel="stylesheet" href="/site.css"></head><body><div class="top"><div class="wrap"><span>Welcome to Shunmu Technology</span><span>${esc(d.s.phone||'')}</span></div></div><header><div class="wrap navbar"><a class="logo" href="/">SMUSU</a>${nav(d.cats)}<button class="mobileBtn" onclick="document.body.classList.toggle('menuOpen')">☰</button></div></header>${body}<footer><div class="wrap footerGrid"><div><b class="logo">SMUSU</b><p>Professional diamond tools for global B2B customers.</p></div><div><b>Products</b><p>${d.cats.map(c=>`<a href="/products/?category=${esc(c.slug)}">${esc(c.name)}</a>`).join('<br>')}</p></div><div><b>Contact</b><p>${esc(d.s.email||'')}<br>${esc(d.s.phone||'')}<br>${esc(d.s.address||'')}</p></div></div></footer></body></html>`,{headers:htmlHeaders})}
const mediaCard=(key,alt)=>`<div class="media">${key?`<img src="/media/${esc(key)}" alt="${esc(alt)}" loading="lazy">`:`<span>${esc(alt)}</span>`}</div>`;
async function home(env){const d=await siteData(env),p=await one(env.DB,"SELECT * FROM pages WHERE page_key='home'"),about=await one(env.DB,"SELECT * FROM pages WHERE page_key='about'"),products=await rows(env.DB,"SELECT p.*,c.name category,(SELECT object_key FROM product_images i WHERE i.product_id=p.id ORDER BY is_primary DESC,sort_order,id LIMIT 1) image FROM products p LEFT JOIN categories c ON c.id=p.category_id WHERE p.status='published' ORDER BY featured DESC,id DESC LIMIT 6"),cases=await rows(env.DB,"SELECT * FROM posts WHERE type='case' AND status='published' ORDER BY id DESC LIMIT 3"),news=await rows(env.DB,"SELECT * FROM posts WHERE type IN ('news','company_news','industry_news') AND status='published' ORDER BY id DESC LIMIT 3");const body=`<section class="hero"><div class="wrap heroGrid"><div><div class="kicker">SHUNMU TECHNOLOGY · SUZHOU</div><h1>${esc(p?.title||'Engineered for the Cut. Built for the Finish.')}</h1><p>${esc(p?.subtitle||'')}</p><a class="btn" href="/products/">Explore Products</a><a class="btn light" href="/feedback/">Request a Quote</a></div><div class="heroVisual"><div class="blade"></div></div></div></section><section><div class="wrap"><div class="sectionHead"><div><div class="kicker orange">PRODUCT FAMILIES</div><h2>Tools built around the application</h2></div></div><div class="grid">${d.cats.map(c=>`<a class="card" href="/products/?category=${esc(c.slug)}">${mediaCard('',c.name)}<div class="cardBody"><h3>${esc(c.name)}</h3></div></a>`).join('')}</div></div></section><section class="soft"><div class="wrap"><div class="sectionHead"><h2>Featured Products</h2></div><div class="grid">${products.map(x=>`<a class="card" href="/products/${esc(x.slug)}/">${mediaCard(x.image,x.name)}<div class="cardBody"><small>${esc(x.category||'')}</small><h3>${esc(x.name)}</h3><p>${esc(x.summary)}</p></div></a>`).join('')||'<div class="empty">No products yet.</div>'}</div></div></section><section><div class="wrap split"><div class="factory">SHUNMU<br>TECHNOLOGY</div><div><div class="kicker orange">COMPANY PROFILE</div><h2>${esc(about?.title||'About SMUSU')}</h2><p>${esc(about?.content||'')}</p><a class="btn" href="/about/">About SMUSU</a></div></div></section><section class="soft"><div class="wrap"><div class="sectionHead"><h2>Cases</h2></div><div class="grid">${cases.map(x=>`<article class="card">${mediaCard(x.cover_key,x.title)}<div class="cardBody"><h3>${esc(x.title)}</h3><p>${esc(x.summary)}</p></div></article>`).join('')||'<div class="empty">Cases will appear here.</div>'}</div></div></section><section><div class="wrap"><div class="sectionHead"><h2>News</h2></div><div class="grid">${news.map(x=>`<article class="card"><div class="cardBody"><h3>${esc(x.title)}</h3><p>${esc(x.summary)}</p></div></article>`).join('')||'<div class="empty">News will appear here.</div>'}</div></div></section>`;return shell({title:'SMUSU | Professional Diamond Tools',desc:p?.content||'',canonical:env.SITE_URL+'/'},body,d)}
async function productList(env,u){const d=await siteData(env),cat=u.searchParams.get('category');const sql=`SELECT p.*,c.name category,c.slug category_slug,(SELECT object_key FROM product_images i WHERE i.product_id=p.id ORDER BY is_primary DESC,sort_order,id LIMIT 1) image FROM products p LEFT JOIN categories c ON c.id=p.category_id WHERE p.status='published' ${cat?'AND c.slug=?':''} ORDER BY p.id DESC`;const items=cat?await rows(env.DB,sql,cat):await rows(env.DB,sql);const filters=`<div class="filters"><a class="chip" href="/products/">All Products</a>${d.cats.map(c=>`<a class="chip ${cat===c.slug?'active':''}" href="/products/?category=${esc(c.slug)}">${esc(c.name)}</a>`).join('')}</div>`;return shell({title:'Products | SMUSU',desc:'Professional diamond tools',canonical:env.SITE_URL+'/products/'},`<section class="pageHero"><div class="wrap"><div class="kicker orange">PRODUCT CENTER</div><h1>Diamond Tools</h1>${filters}</div></section><section><div class="wrap"><div class="grid">${items.map(x=>`<a class="card" href="/products/${esc(x.slug)}/">${mediaCard(x.image,x.name)}<div class="cardBody"><small>${esc(x.category||'')}</small><h3>${esc(x.name)}</h3><p>${esc(x.summary)}</p></div></a>`).join('')||'<div class="empty">No published products.</div>'}</div></div></section>`,d)}
async function productPage(env,slug){const d=await siteData(env),p=await one(env.DB,"SELECT p.*,c.name category FROM products p LEFT JOIN categories c ON c.id=p.category_id WHERE p.slug=? AND p.status='published'",slug);if(!p)return new Response('Not Found',{status:404});const images=await rows(env.DB,"SELECT * FROM product_images WHERE product_id=? ORDER BY is_primary DESC,sort_order,id",p.id),specs=await rows(env.DB,"SELECT * FROM product_specs WHERE product_id=? ORDER BY sort_order,id",p.id);return shell({title:p.seo_title||`${p.name} | SMUSU`,desc:p.seo_description||p.summary,canonical:`${env.SITE_URL}/products/${p.slug}/`},`<main class="wrap product"><p class="crumb">Home / Products / ${esc(p.category||'')}</p><div class="productGrid"><div class="gallery">${images.map(i=>mediaCard(i.object_key,i.alt_text||p.name)).join('')||mediaCard('',p.name)}</div><div><div class="kicker orange">${esc(p.category||'DIAMOND TOOLS')}</div><h1>${esc(p.name)}</h1><p class="lead">${esc(p.summary)}</p><p>${esc(p.description)}</p><a class="btn" href="/feedback/?product=${encodeURIComponent(p.name)}">Request Quote</a></div></div><section><h2>Specifications</h2><table>${specs.map(s=>`<tr><th>${esc(s.name)}</th><td>${esc(s.value)}</td></tr>`).join('')||'<tr><td>No specifications yet.</td></tr>'}</table></section></main>`,d)}
async function listPosts(env,u,type,title){const d=await siteData(env),filter=u.searchParams.get('type');let items;if(type==='news'&&filter)items=await rows(env.DB,"SELECT * FROM posts WHERE type=? AND status='published' ORDER BY id DESC",filter);else if(type==='news')items=await rows(env.DB,"SELECT * FROM posts WHERE type IN ('news','company_news','industry_news') AND status='published' ORDER BY id DESC");else items=await rows(env.DB,"SELECT * FROM posts WHERE type=? AND status='published' ORDER BY id DESC",type);return shell({title:`${title} | SMUSU`,canonical:`${env.SITE_URL}/${title.toLowerCase()}/`},`<section class="pageHero"><div class="wrap"><h1>${title}</h1></div></section><section><div class="wrap"><div class="grid">${items.map(x=>`<article class="card">${mediaCard(x.cover_key,x.title)}<div class="cardBody"><h3>${esc(x.title)}</h3><p>${esc(x.summary)}</p><div class="copy">${esc(x.content)}</div></div></article>`).join('')||'<div class="empty">No content yet.</div>'}</div></div></section>`,d)}
async function videoPage(env){const d=await siteData(env),items=await rows(env.DB,"SELECT * FROM videos WHERE status='published' ORDER BY id DESC");return shell({title:'Videos | SMUSU',canonical:env.SITE_URL+'/videos/'},`<section class="pageHero"><div class="wrap"><h1>Videos</h1></div></section><section><div class="wrap"><div class="grid">${items.map(v=>`<article class="card"><div class="media">${v.video_key?`<video controls preload="metadata" ${v.poster_key?`poster="/media/${esc(v.poster_key)}"`:''}><source src="/media/${esc(v.video_key)}" type="video/mp4"></video>`:'VIDEO'}</div><div class="cardBody"><h3>${esc(v.title)}</h3><p>${esc(v.description)}</p></div></article>`).join('')||'<div class="empty">No videos yet.</div>'}</div></div></section>`,d)}
async function basic(env,key){const d=await siteData(env),p=await one(env.DB,"SELECT * FROM pages WHERE page_key=?",key);return shell({title:`${p?.title||key} | SMUSU`,canonical:`${env.SITE_URL}/${key}/`},`<section class="pageHero"><div class="wrap"><h1>${esc(p?.title||key)}</h1><p>${esc(p?.subtitle||'')}</p></div></section><section><div class="wrap"><p>${esc(p?.content||'')}</p></div></section>`,d)}
async function feedback(env,u){const d=await siteData(env),product=u.searchParams.get('product')||'';return shell({title:'Feedback | SMUSU',canonical:env.SITE_URL+'/feedback/'},`<section class="pageHero"><div class="wrap"><h1>Feedback / Inquiry</h1><p>Tell us what you need. Our team will contact you.</p></div></section><section><div class="wrap"><form class="feedback" method="post" action="/api/inquiry"><input name="name" required placeholder="Name"><input name="company" placeholder="Company"><input name="country" placeholder="Country"><input type="email" name="email" required placeholder="Email"><input name="phone" placeholder="WhatsApp / Phone"><input name="product" value="${esc(product)}" placeholder="Interested Product"><textarea name="message" required placeholder="Message"></textarea><input class="hp" name="website" tabindex="-1" autocomplete="off"><button class="btn" type="submit">Send Inquiry</button></form></div></section>`,d)}
async function inquiry(req,env){const f=await req.formData();if(f.get('website'))return new Response('OK');const name=String(f.get('name')||'').trim(),email=String(f.get('email')||'').trim(),message=String(f.get('message')||'').trim(),product=String(f.get('product')||'').trim();if(!name||!email||!message)return new Response('Required fields missing',{status:400});await env.DB.prepare("INSERT INTO inquiries(name,email,company,country,phone,message,status) VALUES(?,?,?,?,?,?, 'new')").bind(name,email,String(f.get('company')||''),String(f.get('country')||''),String(f.get('phone')||''),product?`Product: ${product}\n${message}`:message).run();return Response.redirect(`${env.SITE_URL}/feedback/?sent=1`,303)}
async function media(req,env){const key=decodeURIComponent(new URL(req.url).pathname.slice(7)),o=await env.MEDIA.get(key);if(!o)return new Response('Not Found',{status:404});const h=new Headers();o.writeHttpMetadata(h);h.set('etag',o.httpEtag);return new Response(req.method==='HEAD'?null:o.body,{headers:h})}
// ADMIN API: products, categories, product images/specs, posts, videos, inquiries
async function api(req,env){const u=new URL(req.url);if(u.pathname==='/api/inquiry'&&req.method==='POST')return inquiry(req,env);if(u.hostname!==env.ADMIN_HOST)return json({error:'Forbidden'},403);
 if(u.pathname==='/api/admin/categories'&&req.method==='GET')return json(await rows(env.DB,"SELECT * FROM categories ORDER BY sort_order,id"));
 if(u.pathname==='/api/admin/products'&&req.method==='GET')return json(await rows(env.DB,"SELECT * FROM products ORDER BY id DESC"));
 if(u.pathname==='/api/admin/products'&&req.method==='POST'){const b=await req.json(),name=String(b.name||'').trim(),slug=slugify(b.slug||name);const r=await env.DB.prepare("INSERT INTO products(name,slug,category_id,summary,description,status,seo_title,seo_description,updated_at) VALUES(?,?,?,?,?,?,?,?,?)").bind(name,slug,b.category_id||null,b.summary||'',b.description||'',b.status||'draft',b.seo_title||'',b.seo_description||'',new Date().toISOString()).run();return json({id:r.meta.last_row_id})}
 let m=u.pathname.match(/^\/api\/admin\/products\/(\d+)$/);if(m&&req.method==='PUT'){const b=await req.json();await env.DB.prepare("UPDATE products SET name=?,slug=?,category_id=?,summary=?,description=?,status=?,seo_title=?,seo_description=?,updated_at=? WHERE id=?").bind(b.name,slugify(b.slug||b.name),b.category_id||null,b.summary||'',b.description||'',b.status||'draft',b.seo_title||'',b.seo_description||'',new Date().toISOString(),+m[1]).run();return json({success:true})}
 m=u.pathname.match(/^\/api\/admin\/products\/(\d+)\/images$/);if(m&&req.method==='GET')return json(await rows(env.DB,"SELECT * FROM product_images WHERE product_id=? ORDER BY is_primary DESC,sort_order,id",+m[1]));if(m&&req.method==='POST'){const pid=+m[1],p=await one(env.DB,"SELECT name FROM products WHERE id=?",pid),fd=await req.formData(),f=fd.get('file');if(!p||!f||typeof f==='string')return json({error:'Invalid upload'},400);const ext=(f.name.split('.').pop()||'jpg').replace(/[^a-z0-9]/gi,'').toLowerCase(),key=`products/${pid}/${crypto.randomUUID()}.${ext}`;await env.MEDIA.put(key,f.stream(),{httpMetadata:{contentType:f.type}});const c=await one(env.DB,"SELECT COUNT(*) total FROM product_images WHERE product_id=?",pid),r=await env.DB.prepare("INSERT INTO product_images(product_id,object_key,alt_text,sort_order,is_primary) VALUES(?,?,?,?,?)").bind(pid,key,p.name,+c.total+1,+c.total===0?1:0).run();return json({id:r.meta.last_row_id,key})}
 m=u.pathname.match(/^\/api\/admin\/products\/(\d+)\/images\/(\d+)\/primary$/);if(m&&req.method==='PUT'){await env.DB.batch([env.DB.prepare("UPDATE product_images SET is_primary=0 WHERE product_id=?").bind(+m[1]),env.DB.prepare("UPDATE product_images SET is_primary=1 WHERE id=? AND product_id=?").bind(+m[2],+m[1])]);return json({success:true})}
 m=u.pathname.match(/^\/api\/admin\/products\/(\d+)\/images\/(\d+)$/);if(m&&req.method==='DELETE'){const x=await one(env.DB,"SELECT object_key FROM product_images WHERE id=? AND product_id=?",+m[2],+m[1]);if(x){await env.MEDIA.delete(x.object_key);await env.DB.prepare("DELETE FROM product_images WHERE id=?").bind(+m[2]).run()}return json({success:true})}
 m=u.pathname.match(/^\/api\/admin\/products\/(\d+)\/specs$/);if(m&&req.method==='GET')return json(await rows(env.DB,"SELECT * FROM product_specs WHERE product_id=? ORDER BY sort_order,id",+m[1]));if(m&&req.method==='POST'){const b=await req.json(),c=await one(env.DB,"SELECT COUNT(*) total FROM product_specs WHERE product_id=?",+m[1]);const r=await env.DB.prepare("INSERT INTO product_specs(product_id,name,value,sort_order) VALUES(?,?,?,?)").bind(+m[1],b.name,b.value,+c.total+1).run();return json({id:r.meta.last_row_id})}
 m=u.pathname.match(/^\/api\/admin\/products\/(\d+)\/specs\/(\d+)$/);if(m&&req.method==='DELETE'){await env.DB.prepare("DELETE FROM product_specs WHERE id=? AND product_id=?").bind(+m[2],+m[1]).run();return json({success:true})}
 if(u.pathname==='/api/admin/posts'&&req.method==='GET')return json(await rows(env.DB,"SELECT * FROM posts ORDER BY id DESC"));if(u.pathname==='/api/admin/posts'&&req.method==='POST'){const b=await req.json(),r=await env.DB.prepare("INSERT INTO posts(type,title,slug,summary,content,cover_key,status,seo_title,seo_description,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?)").bind(b.type,b.title,slugify(b.slug||b.title),b.summary||'',b.content||'',b.cover_key||'',b.status||'draft',b.seo_title||'',b.seo_description||'',new Date().toISOString()).run();return json({id:r.meta.last_row_id})}
 if(u.pathname==='/api/admin/videos'&&req.method==='GET')return json(await rows(env.DB,"SELECT * FROM videos ORDER BY id DESC"));if(u.pathname==='/api/admin/videos'&&req.method==='POST'){const fd=await req.formData(),f=fd.get('file'),title=String(fd.get('title')||'Video');if(!f||typeof f==='string')return json({error:'No video'},400);const ext=(f.name.split('.').pop()||'mp4').replace(/[^a-z0-9]/gi,''),key=`videos/${crypto.randomUUID()}.${ext}`;await env.MEDIA.put(key,f.stream(),{httpMetadata:{contentType:f.type}});const r=await env.DB.prepare("INSERT INTO videos(title,slug,description,video_key,status) VALUES(?,?,?,?,?)").bind(title,slugify(title),String(fd.get('description')||''),key,String(fd.get('status')||'draft')).run();return json({id:r.meta.last_row_id,key})}
 if(u.pathname==='/api/admin/inquiries'&&req.method==='GET')return json(await rows(env.DB,"SELECT * FROM inquiries ORDER BY id DESC LIMIT 200"));return json({error:'Not Found'},404)}
async function app(req,env){const u=new URL(req.url);if(['/site.css','/admin.css','/admin.js','/robots.txt'].includes(u.pathname))return env.ASSETS.fetch(req);if(u.pathname.startsWith('/media/'))return media(req,env);if(u.pathname.startsWith('/api/'))return api(req,env);if(u.hostname===env.ADMIN_HOST){const a=new URL('/admin.html',req.url);return env.ASSETS.fetch(new Request(a.toString(),{headers:req.headers}))}if(u.pathname==='/')return home(env);if(/^\/products\/?$/.test(u.pathname))return productList(env,u);let m=u.pathname.match(/^\/products\/([^/]+)\/?$/);if(m)return productPage(env,decodeURIComponent(m[1]));if(/^\/about\/?$/.test(u.pathname))return basic(env,'about');if(/^\/contact\/?$/.test(u.pathname))return basic(env,'contact');if(/^\/feedback\/?$/.test(u.pathname))return feedback(env,u);if(/^\/news\/?$/.test(u.pathname))return listPosts(env,u,'news','News');if(/^\/cases\/?$/.test(u.pathname))return listPosts(env,u,'case','Cases');if(/^\/videos\/?$/.test(u.pathname))return videoPage(env);return new Response('Not Found',{status:404})}
export default{async fetch(req,env){try{return await app(req,env)}catch(e){console.error('SMUSU_V23',e?.stack||e);return new Response(`SMUSU V2.3 Error\n${e?.message||e}`,{status:500})}}};
